import ScrollStackView from '@/components/ScrollStackView';
import { Text } from 'react-native';

export default function Tab3() {
  return (
    <ScrollStackView title="Tab 3 Nav">
      <Text>Tab 3 blah blah...</Text>
    </ScrollStackView>
  );
}
